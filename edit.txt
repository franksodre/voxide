update: apparently zoxide prefers matching names over ranks

so i should look for matching names
and when something isn't that explicit choose ranks instead.

it's like:
  - vx path
  - database: ~/Documents/path -- path here
  - database: ~/Documents/path_2 -- path here with higher rank

am using this function:

pub fn (db Database) find_match(path string) ?File {

	files := db.find_files(path) or { panic(err) }

	if files.len != 0 {

		mut matches := filter_files_by_name(files)

		matches.sort_with_compare(|a, b| sort(a, b))

		return matches[0]

	}

	return none

}



What it does is obvious, but I wrote when I had a wrong mental model of zoxide's frecency algorithm, the wrong part here is the `sorting` 'cause I sort by score so perhaps I should return an array and in the update/add function do the sorting now based on the
