update: apparently zoxide prefers matching names over ranks

so i should look for matching names
and when something isn't that explicit choose ranks instead.

it's like:
  - vx path
  - database: ~/Documents/path -- path here
  - database: ~/Documents/path_2 -- path here with higher rank

~/Documents/path/path2/file.x
-> /home/user/Documents/path/path2/file.x
-> home user Documents path path2 file.x

cleaning:

# Structure & Organization

Separate concerns: Move database operations into the database module, keep CLI logic in main
Extract the Config struct to its own file or at least group all CLI-related code together
Remove commented-out code - it clutters the file and belongs in version control history

# Error Handling

Don't panic on database open - return a proper error message to the user instead
Be consistent: Some errors use or { panic() }, others use or { eprintln() exit(1) } - pick one pattern
Add context to errors: Instead of just err, say "Failed to open database: ${err}"

# Logic Flow

Move println(conn.select_all()!) after the defer block or remove it - it's called before any operations happen
The defer should be immediately after opening the connection, not after other operations
Check if ANY flags were provided - right now if you run it with no flags, it just dumps the empty config

# Specific Issues

The query implementation is a stub - either implement it or remove it
add_or_update_file always passes 1.0 as score - should this be configurable or calculated?
No validation on paths before adding them (do they exist? are they absolute?)
The tool needs a purpose: Add a help message explaining what this tool does

# Better Design Patterns

Create a run() function that takes the config and connection - makes main() cleaner and testable
Add a validate() method to Config to check that exactly one operation was specified
Consider using subcommands (like voxide add <path>) instead of flags - more intuitive CLI

# Minor Improvements

Fix indentation inconsistency (mix of tabs/spaces in the struct)
Add meaningful variable names instead of f for file
The timestamp conversion could be time.now().unix_time() (check V's API)
Add a -h/--help flag handler

MUST DO:
  handle when i'm already in the target dir.

if i'm returing a Optional i shouldn't interpret it like an error, so don't panic or nothing like that in options
if the `find` function doesn't return nothing(and it's ok in you're case) just handle it proprely(Handling properly doesn't mean throwing errors)
a could thing to do is that you may send an error(msg) at the end but handling all the edge cases until there. - i think
